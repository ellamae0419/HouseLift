PORT=3001
ACCESSTOKEN_SECRET="replace-with-a-long-random-string"
REFRESHTOKEN_SECRET="replace-with-a-different-long-random-string"

# DB CONFIG
DB_HOST='localhost'
DB_USER='root'
DB_PASSWORD=''
DB_DATABASE='house_lift'

ALLOWED_ORIGIN='http://localhost:1234'
CLIENT_URL='http://localhost:1234'

# EMAIL (Resend) CONFIG - leave blank in dev to print emails to the server console instead.
# Without a verified custom domain on Resend, leave EMAIL_FROM blank too
# (falls back to onboarding@resend.dev).
RESEND_API_KEY=''
EMAIL_FROM=''

# SMS (Semaphore) CONFIG - leave blank in dev to print the OTP to the server console instead
SEMAPHORE_API_KEY=''
SEMAPHORE_SENDER_NAME=''
